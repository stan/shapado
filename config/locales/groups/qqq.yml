# Messages for Message documentation (Message documentation)
# Exported from translatewiki.net
# Export driver: syck-pecl
# Author: Discon
# Author: EugeneZelenko
# Author: Nemo bis
# Author: Patcito
# Author: Shirayuki
qqq: 
  activerecord: 
    attributes: 
      group: 
        daily_cap: Daily limit.
        description: "{{Identical|Description}}"
        domain: "{{Identical|Domain}}"
        group_created_by: "\"created by admin\"\n\nhttp://shapado.com/groups/sandbox"
        language: "{{Identical|Default language}}"
        legend: "{{Identical|Legend}}"
        logo: "{{Identical|Logo}}"
        name: "{{Identical|Name}}"
        subdomain: "{{Identical|URL}}"
    models: 
      group: "{{Identical|Group}}"
      groups: "{{Identical|Group}}"
  groups: 
    group: 
      about: "{{Identical|About}}"
      close: "{{Identical|Close}}"
      close_group: "{{Identical|Close this group}}"
    index: 
      actives: "{{Identical|Active}}"
      description: The whole sentence at the end reads "You can also create your own group".
      name: "{{Identical|Name}}"
      pendings: "{{Identical|Pending}}"
      title: "{{Identical|Group}}"
    show: 
      close_group: "{{Identical|Close this group}}"
